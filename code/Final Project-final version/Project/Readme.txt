1. Open terminal and enter into the file directory
2. input ./aa.sh  (sometimes need to input (chmod +x aa.sh) then input ./aa.sh )
3. A file named b.txt will be generated by “result.gdb” as an intermediate file.
4 Then a file named “c.txt”will be generated and store all of the values for variables in each excitable line, and the local variables’ name will be shown in “d.txt”.
5.All code for teacher mode are stored in teacher folder
6.All code for student mode are stored in new2 folder
7. All database file are stored in database folder

